/*
    -> This design pattern ensure that a class has only one instance and provide a global point of access to it. It is used when we want to control the access to some resource, such as a database or a file, and we want to ensure that there is only one instance of that resource in the system.

    -> in simple terms it checks the how is the structure of the class's object

    -> it focuses on adding new functionality without any need to modify entire system.
*/